apiVersion: v1
kind: Service
metadata:
  labels:
    app: iot
  name: website-service
spec:
  type: ClusterIP
  selector:
    app: iot
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8888